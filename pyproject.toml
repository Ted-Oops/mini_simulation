[project]
name = "mini-simulation"
version = "0.3.0"
description = "Lightweight multi-agent market simulation sandbox with a batch-based price-time-priority order book."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.10,<4.0",
]

[tool.uv]
package = false
