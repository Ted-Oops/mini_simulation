[project]
name = "mini-simulation"
version = "0.1.0"
description = "Lightweight multi-agent market simulation sandbox for baseline and LLM-driven experiments."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.10,<4.0",
]

[tool.uv]
package = false
